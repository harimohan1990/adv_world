[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
env =
    SECRET_KEY=test_secret_key
    ALGORITHM=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=30
    DATABASE_URL=sqlite+aiosqlite:///:memory:
